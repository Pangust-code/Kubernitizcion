# ─── Dependencias de la aplicación ──────────────────────────────────────────
# Fijar versiones exactas garantiza reproducibilidad entre entornos.

# Framework web
fastapi==0.115.0
uvicorn[standard]==0.32.0          # ASGI server con soporte websockets y reload

# Base de datos / caché
redis==5.1.1                       # Cliente Redis asíncrono

# Métricas (Prometheus)
prometheus-client==0.21.0          # Expone métricas en /metrics

# Logging estructurado
python-json-logger==2.0.7          # Formatea logs como JSON (más fácil de indexar)

# Utilidades
pydantic-settings==2.5.2           # Configuración desde variables de entorno
httpx==0.27.2                      # Cliente HTTP async (para tests y health checks)
