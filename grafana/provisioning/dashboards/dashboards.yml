apiVersion: 1

providers:
  - name: 'Demo Dashboards'
    orgId: 1
    folder: 'Demo'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30     # Recarga el JSON si cambia en disco
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
